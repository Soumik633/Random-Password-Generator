# Random-Password-Generator
A simple Python password generator that creates a secure 10-character password using random selection and string operations. It combines letters and digits, loops through characters, and uses the random module to ensure unpredictability and uniqueness.
